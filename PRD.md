# PRD - Длина волны (Wavelength online)

Статус: живой документ. Раздел «Что есть» описывает текущее состояние прод-версии,
раздел «Планы» - незакреплённый backlog идей (приоритеты не расставлены).

Прод: https://wavesite-rho.vercel.app
Репозиторий: https://github.com/boundlessend/wave_site

## 1. Цель

Онлайн-версия настольной «Длины волны» (Wavelength): открыл ссылку - играешь компанией,
у каждого своё устройство. Без регистрации, без своего сервера, бесплатный хостинг.

## 2. Что уже есть (реализовано)

### Геймплей
- Два режима: соревнование (две команды) и кооператив (все против колоды из 7 карт).
- Полные правила: фаза телепата, фаза команды (стрелка), фаза «слева/справа» для соперников,
  раскрытие, начисление очков (2-4 за зону, +1 за верную сторону), правило догоняющего.
- Победа по 10+ очкам при перевесе над соперником (равенство на вершине - доигрываем);
  в коопе центр даёт 3 очка, итог оценивается по шкале.
- Встроенный русский набор карт-противоположностей плюс свои пары (localStorage вкладки).
- Таблица итогов сыгранных раундов; карты внутри партии не повторяются.

### Мультиплеер и синхронизация
- Комнаты по ссылке-приглашению, realtime через Supabase (broadcast + presence).
- Модель host-authoritative без своего сервера; авторитет (host) выбирается по presence
  и мигрирует при отключении.
- Вышедшие игроки удаляются автоматически с отсрочкой: перезагрузка вкладки возвращает
  игрока на его место (sessionStorage). Зависший раунд можно пропустить, выход в лобби.
- Лобби: автобаланс команд при входе, выход из комнаты, кик любого участника; опасные
  действия (кик, пропуск раунда, выход в лобби) требуют подтверждения.
- Размер комнаты ограничен (MAX_PLAYERS), чтобы флуд join не раздувал состояние.
- Версия протокола в каждом сообщении: устаревший клиент получает баннер «обнови страницу».

### Безопасность (протокол v2)
- Все realtime-сообщения и presence зашифрованы AES-GCM; ключ выводится (HKDF) из секрета
  комнаты, который живёт в hash ссылки (`#k=`) и не уходит на сервер. Знающий только код
  комнаты не прочитает трафик и не подделает состояние/действия/host.
- Против replay/реордеринга: окно свежести, отсев повторов, монотонный seq состояния;
  presence-рекорды с TTL и периодическим перевыпуском.
- Мишень по схеме commit-reveal: телепат публикует хеш (мишень+nonce) до подсказки, хост
  сверяет его при раскрытии - мишень нельзя «выбрать» после просмотра стрелки; сама она
  до раскрытия существует только на устройстве телепата.
- Роль актёра привязана к устройству-отправителю по presence, и это fail-closed: без
  presence действие отклоняется. Состояние принимается только от признанного хостом
  клиента и проверяется по форме; при расхождении presence (split-brain) более поздний
  хост уступает более раннему.
- Мишень генерируется CSPRNG; commit сверяется и на действиях самого хоста.
- Входящие ограничены по частоте двумя бюджетами: общим (защита CPU) и на отправителя
  (чтобы один клиент не выел лимит остальных). Строки/числа клампятся на хосте.
  Код комнаты 6 символов на CSPRNG.
- Security-заголовки в `vercel.json`: CSP без `unsafe-inline` (React ставит стили через
  CSSOM), запинен на поддомен проекта, plus HSTS и `form-action 'none'`. Домен Supabase
  синхронизируется командой `npm run sync:csp`. Регистрация в Supabase отключена.
- Принято by design: обладатели ссылки-приглашения доверены (per-device ключей нет);
  клиент-хост технически может исказить состояние (host-authoritative).

### Дизайн и UX
- Редакторский («эдиториал») вид: светлая бумага, строгая типографика (Unbounded / Inter /
  JetBrains Mono self-host, сериф - системная Georgia), красно-синий акцент, жёсткая сетка.
- Тёмная тема: по умолчанию как в системе, переключатель (авто/светлая/тёмная) рядом с фоном.
- OG-теги и превью-карточка для шеринга ссылки в мессенджерах; QR-код комнаты для входа
  с телефона; manifest и иконки для добавления на домашний экран.
- Переключаемый фоновый 3D (чистый three.js, ленивый чанк после первого кадра): 5 сцен на
  тему волны (ридж-волны по умолчанию, полутон, фигуры, каркас, рябь) + «без фона».
  Анимация геометрии - в вершинных шейдерах, CPU только тикает время. Выбор в localStorage.
- Анимации (Motion), звук, конфетти на победе.
- Доступность: шкала как slider (стрелки/Home/End), не-цветовые метки команд, `prefers-reduced-motion`.
- Адаптив: мобильная и десктоп-раскладки.

### Инфраструктура и качество
- React 19 + Vite + TypeScript (строгий). Логика игры - чистые функции, self-check'и правил и движка.
- CI (GitHub Actions): `npm run check` (tsc вкл. selfcheck'и и e2e как код, self-check'и,
  линт с `--deny-warnings`) + build + Playwright на каждый push/PR: смоук раунда и
  многовкладочные сценарии (роли, кик, восстановление после перезагрузки, свои карточки).
  Кеш браузеров, отчёт-артефакт при падении. Dependabot для зависимостей.
- Авто-деплой на Vercel при push в `main`. BSD 3-Clause, README актуален.

### Тарифы и ограничения (текущие)
- Vercel Hobby: статика на CDN, лимит по трафику (~100 ГБ/мес), не по размеру бандла.
- Supabase Free: лимиты на realtime-сообщения/одновременные соединения; фон и анимации
  клиентские, на Supabase не влияют. Realtime через `@supabase/realtime-js` (без лишних частей SDK).
- Бандл: стартовая загрузка ~140 КБ gzip (вендоры react/supabase/motion — отдельными чанками,
  правки кода их не инвалидируют); 3D-сцена отдельным ленивым чанком ~134 КБ gzip, грузится
  после отрисовки игры и только если фон включён. QR-кодер (~7 КБ gzip) и конфетти - лениво.

## 3. Планы / backlog (черновые идеи, разгрести позже)

### 3.1 Оптимизация
- Полный аудит Lighthouse / Core Web Vitals; `size-adjust` для fallback-метрик шрифтов (CLS).
- Инстансинг для точечных сцен 3D-фона (анимация уже на шейдерах).

### 3.2 Новые фичи (геймплей)
- Больше встроенных карт и категорий (свои пары уже можно добавлять).
- Обмен наборами карточек между игроками комнаты (сейчас набор локальный).
- Опциональный таймер на подсказку/ход.
- Кастомные названия команд, простые аватары/эмодзи игроков.
- Лёгкие реакции (эмодзи) во время раунда.

### 3.3 UX и качество
- Шеринг картинки с результатом партии (таблица итогов уже есть).
- Короткий онбординг/туториал для новичков; подсказки прямо в фазах.
- Локализация (i18n), для начала RU/EN.

### 3.4 Техническое и надёжность
- E2E на реальном Supabase (сейчас многоклиентные сценарии идут через BroadcastChannel).
- Восстановление после долгих обрывов связи (перезагрузка вкладки уже переживается).
- Телеметрия по желанию (анонимная, без личных данных) для понимания узких мест.
- Per-device ключи / серверная авторизация ролей (сейчас приглашённые доверены by design).
