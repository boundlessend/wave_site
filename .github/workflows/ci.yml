name: CI

on:
  push:
    branches: [main]
  pull_request:

# токен только на чтение: воркфлоу (в т.ч. из форков) ничего не изменит
permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Self-check правил и движка
        run: |
          node src/game/rules.selfcheck.ts
          node src/game/engine.selfcheck.ts
      - name: Линт
        run: npm run lint
      - name: Сборка
        run: npm run build
