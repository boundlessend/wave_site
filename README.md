# Длина волны

Онлайн-версия настольной игры Wavelength: открываешь ссылку и играешь компанией, у каждого своё устройство.

Прод: https://wavesite-rho.vercel.app

## Как играть

Создай комнату, отправь ссылку друзьям. Телепат тайно видит цветную зону на шкале и даёт словесную подсказку между двумя противоположностями (без чисел и однокоренных слов). Его команда двигает стрелку к центру зоны; вторая команда угадывает, слева или справа от стрелки центр (+1 очко). Телепат открывает экран: чем ближе к центру, тем больше очков (2-4). Побеждает команда с 10+ очками. В кооперативе все играют вместе против колоды из 7 карт.

## Возможности

- Комнаты по ссылке, синхронизация в реальном времени, у каждого своё устройство.
- Авторитет состояния (host) выбирается по presence и мигрирует при отключении; вышедшие игроки удаляются автоматически, зависший раунд можно пропустить.
- Секрет мишени живёт только на устройстве телепата до раскрытия; ходовые действия проверяются по роли актёра.
- Присоединение по ссылке-приглашению; в ней секрет комнаты (в hash, не уходит на сервер), которым подписываются все realtime-сообщения - чужой без ссылки не подделает состояние и не влезет в игру.
- Соревновательный и кооперативный режимы, правило догоняющего.
- Космический вид: 3D-звёздное небо, анимации, звук, празднование победы; учтён `prefers-reduced-motion`.
- Шкала управляется и мышью/тачем, и клавиатурой (стрелки).

## Стек

React + Vite + TypeScript. Realtime: Supabase (broadcast + presence). Анимации: Motion + GSAP. 3D-фон: Three.js + React Three Fiber (drei). Логика игры - чистые функции (`src/game/`), транспорт абстрагирован (`src/net/`, локальный и Supabase).

## Разработка

```bash
npm install
npm run dev
```

Без `.env` приложение работает в локальном режиме (одна вкладка, для разработки). Для онлайна скопируй `.env.example` в `.env` и заполни:

- `VITE_SUPABASE_URL` - URL проекта Supabase
- `VITE_SUPABASE_KEY` - публичный (publishable) ключ

## Проверки

```bash
npm run check   # tsc + self-check'и правил и движка
npm run lint    # oxlint
npm run build   # прод-сборка
```

Те же шаги гоняются в CI (GitHub Actions) на каждый push и PR.

## Деплой

Авто-деплой при push в `main` (Vercel). Вручную: `npx vercel --prod`.
